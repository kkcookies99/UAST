 var XXX = function(x) {
    let flag = 0>x
    let arr = x.toString().split('');
    flag&&arr.shift();
    let tem = Number(arr.XXX().join(''))
    let res = flag?0-tem:tem;
    return (Math.pow(-2,31)<=res&&res<=(Math.pow(2,31)-1))?res:0;
};

