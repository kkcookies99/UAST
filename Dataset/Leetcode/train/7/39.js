var XXX = function(x) {
  let flag = true,list=[];
   var a = x.toString();
   if(x>=0){
        list = a.split('').XXX();
        return list.join('')*1; 
   }
   else{
       list = a.split('');
       list.shift();
       list = list.XXX();
       return list.join('')*(-1); 
   }
};

